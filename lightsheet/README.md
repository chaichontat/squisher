# squisher-lightsheet

Lightsheet stitching workflow commands for metadata positioning, rough center-z phase alignment,
registration, fusion, pyramid generation, and QC.

```bash
uv run --package squisher-lightsheet lightsheet --help
```

Channel-alignment workflows are intentionally out of scope for this first package pass.
