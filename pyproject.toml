[tool.uv.workspace]
members = ["squisher", "lightsheet", "lightsheet-psf", "deconv", "segment"]

[tool.pytest.ini_options]
markers = ["gpu: requires a CUDA-capable GPU"]

[tool.uv]
override-dependencies = ["scipy>=1.16.0"]
